from setuptools import setup

setup(
    name="tm_gym",
    version='0.0.1',
    install_requires=['gym',
                      'numpy',
                      'matplotlib',
                      'torch']
)
